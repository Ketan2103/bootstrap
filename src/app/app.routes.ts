import { Routes } from '@angular/router';
import { ManageSideOptionsComponent } from './manage-side-options/manage-side-options.component';

export const routes: Routes = [
    // { path: "m", component: ManageSideOptionsComponent }
];
